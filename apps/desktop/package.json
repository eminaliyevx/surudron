{
  "name": "@surudron/desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "electrobun build --env=stable",
    "check-types": "tsc --noEmit",
    "dev": "electrobun dev --watch"
  },
  "dependencies": {
    "@surudron/env": "workspace:*",
    "@surudron/server": "workspace:*",
    "electrobun": "^1.18.1"
  },
  "devDependencies": {
    "@surudron/config": "workspace:*"
  }
}
